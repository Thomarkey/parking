import { ParkingOpenFilterPipe } from './parking-open-filter-pipe';

describe('ParkingOpenFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingOpenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
